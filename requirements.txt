(things installed so far)
pip install tkinter
pip install numpy
pip install pillow
pip install pydicom
pip install matplotlib
pip install scipy


(things left to install)
pip install scikit.image
pip install scikit-learn
